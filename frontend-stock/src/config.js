const urlServer = "http://localhost:3000";

const urlServerProducts = `${urlServer}/api/products`;
const urlServerMovements = `${urlServer}/api/movements`;

export const config = {
  urlServer,
  urlServerProducts,
  urlServerMovements,
};
